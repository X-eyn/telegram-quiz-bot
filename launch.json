{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: QuizBot",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/quizbot/bot/bot.py",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}